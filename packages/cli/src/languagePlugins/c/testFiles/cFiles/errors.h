#include "thisfiledoesnotexist.h"

// Unnamed enum
enum {
    ONE, TWO, THREE, VIVA, L, ALGERIE
};
int xbox = ONE;

struct {
    int useless; // USELESS?
};

// Non-standard typedef
struct Salut_t {
	int id;
    int waow;
} typedef Salut;

// Syntax error
int x =??;
