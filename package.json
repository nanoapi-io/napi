{
  "name": "nanoapi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:app": "npm run dev --workspace packages/app",
    "dev:cli": "NODE_ENV=development npm run dev --workspace packages/cli",
    "build:cleanup": "rm -rf dist",
    "build:packages": "npm run build --workspaces && cp -r packages/cli/dist dist && cp -r packages/app/dist dist/app_dist",
    "build:copy-dist": "cp -r packages/cli/dist dist && cp -r packages/app/dist dist/app_dist",
    "build": "npm run build:cleanup && npm run build:packages && npm run build:copy-dist",
    "start": "node dist/index.js",
    "lint": "eslint . --fix",
    "lint:check": "eslint .",
    "test": "vitest"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "eslint": "^9.11.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "prettier": "3.3.3",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
